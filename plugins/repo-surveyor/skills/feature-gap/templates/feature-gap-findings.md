# feature-gap findings — `<repo/module name>`

> Survey date: `<YYYY-MM-DD>` · Branch: `<branch>` · Commit: `<short-sha>`
> Severity system: `<detected — NAME (source: path) | Eisenhower → P0–P3>`
> Surface analyzed: `<routes / MCP tools / CLI / API found>` · Generated by repo-surveyor `feature-gap`.
> **Static & recommendation-only (RS-N4): this report maps the surface; it does not measure live
> traffic. Every metric below is a recommendation with a read point, not an observed value.**

## Surface inventory (what was mapped)

- **UI routes:** `<n>` — `<e.g. /login, /dashboard, …>`
- **MCP tools:** `<n>` — `<tool names>`
- **CLI commands:** `<n>` — `<commands>`
- **Public API:** `<n>` — `<endpoints/exported symbols>`

## Summary

- Findings: gaps `<n>` · inconsistencies `<n>` · low-discoverability `<n>` · recommended `<n>` · metrics `<n>`.
- By severity: `<P0..P3 or detected levels>`. Needs-confirmation: `<m>`.
- Top three by leverage: `<one line each>`.

---

## Feature gaps

### `<short title>` — `<severity>` · leverage `<impact ÷ LOE>`
- **Surface element:** `<route / tool name + schema line / command / API symbol>` at `path:NN`.
- **The gap:** `<the missing/half-built capability a user would expect>` (grep-confirmed absent).
- **Trade-off — building it:** `<value>`. **Not building it:** `<the user friction it leaves>`.
- (or "none confirmed")

## Inconsistencies

### `<short title>` — `<severity>` · leverage `<…>`
- **Sibling surfaces:** `<A at pathA:NN>` vs `<B at pathB:MM>` — diverge in `<naming/arg-shape/error-contract/defaults/casing>`.
- **User impact:** `<why the divergence misleads or frustrates>`. **Fix trade-off both ways:** `<…>`.
- (or "none confirmed")

## Low discoverability

### `<short title>` — `<severity>` · leverage `<…>`
- **Feature:** `<the working capability>` at `path:NN` — wired and reachable, but **no** `<doc/help/description/trigger>`.
- **Cost:** `<who never finds it>`. **Cheapest fix:** `<add help text / description / doc pointer>`.
- (or "none confirmed")

## Recommended features (additive)

### `<short title>` — `<severity/priority>` · leverage `<…>`
- **Motivated by:** `<the current surface element(s) that imply it>` at `path:NN`.
- **What / why:** `<the feature and the usability/engagement it targets>`.
- **Trade-off — building:** `<value>`. **Not building:** `<opportunity cost>`.
- (or "none")

## Product metrics to track  (what & why — recommendation-only; placement → signal-map)

### `<feature name>`
- **Metric:** `<adoption | activation | funnel drop-off | task success | error rate>` — read from `<event/surface>` at `path:NN`.
- **Why it tells you the feature works for users:** `<the product question it answers>`.
- **→ signal-map:** `<the instrumentation placement needed for this metric, if any>` (RS-N5).
- (or "none")

---

## Needs confirmation  (ungrounded — questions, not findings; RS-N6)

- `<surface element / path:NN>` — suspected `<gap/inconsistency>`; would confirm: `<the check>`.
- (or "none")

## Cross-references  (other lenses — RS-N5)

- **→ signal-map:** `<metrics that need instrumentation placement>`.
- **→ debt-radar:** `<a "feature" that is actually dead/unwired code>`.
- (or "none")
