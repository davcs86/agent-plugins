# debt-radar findings — `<repo/module name>`

> Survey date: `<YYYY-MM-DD>` · Branch: `<branch>` · Commit: `<short-sha>`
> Severity system: `<detected — NAME (source: path) | Eisenhower → P0–P3 (no repo system detected)>`
> Analysis root: `<path>` · Generated by repo-surveyor `debt-radar` (read-only survey).
> Estimates (LOE, benefit, severity) are labeled projections, not measurements (RS-4).

## Summary

- Findings confirmed: `<n>` across `<k>` categories. Needs-confirmation: `<m>`.
- By severity: `<P0: n>` · `<P1: n>` · `<P2: n>` · `<P3: n>` (or the detected system's levels).
- Top three by leverage: `<one line each — the finding + why it tops the list>`.
- Measured facts this rests on: `<e.g. 4 dead exports (grep-confirmed 0 refs), 1,900-line god file>`.

---

## Swallowed / silenced errors

<!-- The highest-signal category. One row per confirmed site. -->

### `<short title>` — `<severity>` · leverage `<impact ÷ LOE>`
- **Evidence:** `path/to/file.ext:NN` — `<quoted handler / suppression>` (`<symbol>`).
- **What it hides:** `<the failure this silently drops>`.
- **Benefit of fixing:** `<incident class closed / failure made visible>`.
- **LOE:** `<XS|S|M|L|XL>` — assumption: `<blast radius: call-sites, tests, interfaces>`.
- **Trade-off — not fixing:** `<what the debt keeps costing>`. **Fixing:** `<new error path callers must handle / churn / risk>`.

- (or "none confirmed")

## Dead code

### `<short title>` — `<severity>` · leverage `<…>`
- **Evidence:** `path:NN` — `<symbol>`; grep across tree: `<0 inbound refs after excluding dynamic/public-API/test use>`.
- **Benefit of fixing / LOE / Trade-off both ways:** `<…>`.
- (or "none confirmed")

## Code smells / anti-patterns

### `<short title>` — `<severity>` · leverage `<…>`
- **Evidence:** `path:NN–MM` — `<the measured shape: e.g. 1,900-line file / 7-level nesting / 6 duplicated blocks>`.
- **Benefit / LOE / Trade-off both ways:** `<…>`.
- (or "none confirmed")

## Brittle / over-specified logic

### `<short title>` — `<severity>` · leverage `<…>`
- **Evidence:** `path:NN–MM` — `<the enumeration / hard-coded branch set that rots as the domain moves>`.
- **Heuristic/data-driven form it should take:** `<one line>`. **Benefit / LOE / Trade-off:** `<…>`.
- (or "none confirmed")

## Duplication

### `<short title>` — `<severity>` · leverage `<…>`
- **Evidence:** `pathA:NN`, `pathB:MM` `<(+ more)>` — same logic, must change together.
- **Benefit / LOE / Trade-off both ways:** `<…>`.
- (or "none confirmed")

## Contradiction with intent  (defects — reported & routed, never auto-fixed)

### `[⚠ security?]` `<short title>` — `<severity>`
- **Evidence:** code at `path:NN` does `<Y>`; the `<comment/docstring/type>` at `path:MM` asserts `<X>`.
- **Routing:** human triage — is the code or the stated intent correct? Fixing either is out of scope for this survey (RS-2). `<link to signal-map if a missing signal would have caught it>`.
- (or "none confirmed")

## Latent risk

### `<short title>` — `<severity>` · leverage `<…>`
- **Evidence:** `path:NN` — `<TODO/FIXME on a load-bearing path / sleep-based sync / unbounded resource>`.
- **Benefit / LOE / Trade-off both ways:** `<…>`.
- (or "none confirmed")

---

## Needs confirmation  (ungrounded suspicions — questions, not findings; RS-N6)

- `path:NN` — suspected `<category>`; would confirm: `<the specific check>`.
- (or "none")

## Cross-references  (other lenses — RS-N5)

- **→ signal-map:** `<a debt site whose failure is currently invisible — points at an instrumentation gap>`.
- **→ feature-gap:** `<a debt that surfaces as a user-facing inconsistency>`.
- (or "none")
