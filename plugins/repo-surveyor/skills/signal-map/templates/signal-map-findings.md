# signal-map findings — `<repo/service name>`

> Survey date: `<YYYY-MM-DD>` · Branch: `<branch>` · Commit: `<short-sha>`
> Severity system: `<detected — NAME (source: path) | Eisenhower → P0–P3>`
> Observability stack: `<detected lib(s) / "none detected — introducing one is part of the LOE">`
> Generated by repo-surveyor `signal-map`.
> **Static & recommendation-only (RS-N4): every metric/latency below is a signal to *place*, not an
> observed value. Placement is this lens; product what/why is feature-gap's (RS-N5).**

## Summary

- Findings: gaps `<n>` · redundant/noisy `<n>` · feature gauges `<n>` · cardinality hazards `<n>`.
- By severity: `<P0..P3 or detected levels>`. Needs-confirmation: `<m>`.
- Signals to **add**: `<n>` · signals to **remove**: `<n>` (surgical — leverage, not blanket).
- Top three gaps by leverage: `<one line each>`.

---

## Instrumentation gaps  (where a failure/slowdown is currently invisible)

### `<short title>` — `<severity>` · leverage `<impact ÷ LOE>`
- **Point:** `path:NN` — `<error branch / hot path / external call / retry-fallback / feature boundary>`; existing signal: **none**.
- **Add:** `<signal type: structured log(fields…) / counter / histogram / span / event>` using `<detected stack>`.
- **Question it answers:** `<the concrete debugging/perf question>`.
- **Trade-off — without it:** `<the blind spot>`. **With it:** `<runtime/storage/cardinality cost>`.
- (or "none confirmed")

## Redundant / noisy signals  (remove — noise cut is half the job)

### `<short title>` — `<severity>` · leverage `<…>`
- **Signal:** `path:NN` — `<log in tight loop / duplicate of pathMM / debug log in hot path / unactionable metric>`.
- **Remove/replace:** `<action>`. **Trade-off — removing:** `<noise/cost cut>` vs `<visibility lost, if any>`.
- (or "none confirmed")

## Feature-performance gauges  (placement here; what/why → feature-gap)

### `<feature name>` — `<severity>` · leverage `<…>`
- **Gauge:** `<latency histogram / throughput counter / error-rate / saturation>` read at `path:NN`.
- **→ feature-gap:** `<the product metric this serves>` (RS-N5).
- **Trade-off both ways:** `<…>`.
- (or "none")

## Cardinality / cost hazards

### `<short title>` — `<severity>`
- **Hazard:** `path:NN` — `<label/dimension that would explode cardinality: user-id, request-id, raw URL>`.
- **Safer form:** `<bounded label / exemplar / sampled span>`.
- (or "none confirmed")

---

## Needs confirmation  (ungrounded — questions, not findings; RS-N6)

- `path:NN` — suspected `<gap/noise/hazard>`; would confirm: `<the check>`.
- (or "none")

## Cross-references  (other lenses — RS-N5)

- **→ feature-gap:** `<product metrics whose placement is specified above>`.
- **→ debt-radar:** `<a swallowed error whose invisibility is the gap — the handler fix is debt-radar's>`.
- (or "none")
